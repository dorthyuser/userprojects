new_project ASP.NET Core Web API (.NET 8)

Endpoints:
- GET /demos

Configuration:
- Azure Key Vault via AZURE_KEY_VAULT_URI
- PostgreSQL secrets resolved through SecretHelper with environment fallback
- Runs on HTTP port 8080